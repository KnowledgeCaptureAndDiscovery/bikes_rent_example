#!/bin/bash

set -e
checkExitCode() {
if [ $? -ne 0 ]; then
    echo "Error"
    exit 1;
fi
}

BASEDIR=`dirname $0`

. $BASEDIR/io.sh 1 3 2 "$@"

set -x
docker run  -python script.py --inputs $INPUTS1 \
    	--variables $PARAMS1 $PARAMS2 $PARAMS3 

checkExitCode
